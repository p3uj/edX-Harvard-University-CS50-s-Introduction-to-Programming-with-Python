# Get the user's input and repplace the space into three periods.
userInput = input().replace(" ", "...")

"""
Here is the code when we want to break down on
how we can replace the space into three periods.

userInput = userInput.replace(" ", "...")
"""

# Display user's input replacing each space with 3 periods.
print(userInput)