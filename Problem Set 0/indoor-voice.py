# Get the user's input and convert into lowercase and remove whitespace from the str
text = input().lower().strip()

"""
Here is the code when we want to break down on
how to convert the user's input into lowercase
and remove the whitespace from the str

text = text.lower().strip()
"""

# Display user's input
print(text)